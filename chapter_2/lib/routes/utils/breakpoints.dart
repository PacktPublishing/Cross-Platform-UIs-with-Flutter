/// The breakpoint for the standings table.
const mobileResultsBreakpoint = 500.0;

/// The maximum stretch for the result cards.
const maxStretchResultCards = 900.0;

/// The maximum stretch for the standings items.
const maxStretchStandingsCards = 600.0;

/// The maximum allowed width of the circular progress arc.
const maxCircularProgress = 150.0;

/// The maximum allowed width for the contents of the 'next races' page.
const maxNextRacesContents = 450.0;
