name: chapter_2
description: The chapter 2 project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # https://pub.dev/packages/flutter_svg
  flutter_svg: ^0.23.0+1

  # https://pub.dev/packages/google_fonts
  google_fonts: ^2.1.0

  # https://pub.dev/packages/intl
  intl: ^0.17.0

dev_dependencies:
  # https://pub.dev/packages/dart_code_metrics
  dart_code_metrics: ^4.5.0

  # https://pub.dev/packages/flutter_lints
  flutter_lints: ^1.0.4

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/flags/
    - assets/svg/
    - google_fonts/